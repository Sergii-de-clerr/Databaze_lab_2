@model IEnumerable<Databaze_lab_2.Transport>

@{
    ViewData["Title"] = "Таблиця транспорту";
}

<h1>Таблиця транспорту</h1>

<p>
    <a asp-action="Create">Створити новий</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TransportName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TransportName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TransportId">Редагувати</a> |
                <a asp-action="Details" asp-route-id="@item.TransportId">Інформація</a> |
                <a asp-action="Delete" asp-route-id="@item.TransportId">Видалити</a>
            </td>
        </tr>
}
    </tbody>
</table>

<div>
    <a asp-controller="Home" asp-action="Menu">Назад до меню</a>
</div>